# Desktop Assets

Place desktop-shell assets here:

- `icon.icns` for macOS bundle icon
- `icon.ico` for Windows executable icon
- optional splash or branding assets used by the launcher/window shell

The current build scripts tolerate missing icons so development can proceed before final branding assets are ready.
