# UI Refresh Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rework the current Streamlit product into a clearer professional dashboard with a workbench landing page and less alarming admin-mode messaging.

**Architecture:** Add a small UI metadata helper, update the shared CSS layer, simplify the login notice flow, and reframe the main shell around a dedicated `工作台` page and better sidebar grouping.

**Tech Stack:** Streamlit, Python, lightweight helper module, existing product pages.

---
