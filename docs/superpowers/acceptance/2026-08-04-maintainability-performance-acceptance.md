# v0.8.0 可维护性与性能验收

## 范围

- 任务中心终态任务使用仓储分页和状态筛选。
- 任务结果图默认折叠，展开后才读取原图。
- 模板库按组折叠并分页，项目中心历史与回收站分页。
- 部署、架构、维护、版本和 AI 接手文档齐备。

## 自动验收

发布前执行编译检查、专项测试、全量 unittest、Compose 配置检查和 `git diff --check`。最终命令与结果记录在发布提交说明中。

## 人工烟测

- 任务中心：活动任务完整可见，最近任务可切换 10/20/50 条，展开前不显示大图。
- 模板库：默认仅显示工作流分组，展开后仅创建当前页编辑器。
- 项目中心：历史与回收站分页，批量操作仍基于完整记录集合。
- 本地健康检查：`/_stcore/health` 返回成功。

## 风险边界

本次不引入缩略图实体缓存和新依赖；通过懒渲染消除主要首屏成本。真实上游图片调用不属于默认自动验收，避免产生未经授权费用。
