# 默认模型绑定界面验收记录

日期：2026-08-04

## 验收范围

- 文字、视觉、出图模型以三个独立工作区展示，并说明各自用途。
- 每个绑定显示已就绪、能力不匹配、上游缺失或尚未绑定状态。
- 能力分类与默认模型绑定分别保存，默认绑定保存前校验必需能力。
- 上游暂时缺失的旧绑定保留并告警，避免破坏历史配置。
- 发布包不得包含或覆盖服务器 `.env`、运行数据、日志、密钥和 `.superpowers/`。

## 自动化证据

- `python3 -m unittest -v test_provider_models test_deployment_manifest`
- `python3 -m compileall -q . -x '(^|/)(\.git|\.superpowers|data|\.venv|node_modules)(/|$)'`
- `python3 -m unittest discover -v`

## 线上证据

- `https://tulite.nowdn.com/qie/` 登录后进入提供商设置进行交互验收。
- 保存默认绑定并刷新，确认选择持久化。
- 服务器容器健康检查与 HTTPS 状态均需通过后签署。

## 结论

通过。

- 专项与部署清单：21 项通过。
- 全量回归：270 项通过，0 失败。
- Python 编译检查：通过。
- 线上页面：三列绑定、状态提示、两个独立保存入口均已显示。
- 持久化：保存后重新登录，三类默认模型保持不变。
- 响应式：390 × 844 视口无横向溢出。
- 发布安全：自动化打开发布压缩包，确认不包含 `.env`、`data/`、日志、密钥或 `.superpowers/`。

## 视觉模型补充回归

- 普通 `chatcompletion` 模型只推断为文字能力；视觉能力必须有明确模型线索或用户分类。
- 视频生成模型不会进入视觉理解候选。
- 当前提供商目录不存在的旧视觉绑定保留展示，但禁止作为新配置保存。
- 任务级文字模型覆盖不会改写提供商的视觉模型绑定。
